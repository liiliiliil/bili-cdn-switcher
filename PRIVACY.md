# 隐私说明

Bili CDN Switcher 不会把用户数据传给开发者、第三方分析服务或广告商，也不出售数据。扩展不包含遥测、广告、远程代码或开发者控制的服务器。

为了完成用户明确启用的 CDN 观测、测速和切换功能，扩展只在用户设备本机处理以下信息：

- `chrome.storage.local`：保存启停、自动/手动模式、自动重新优选档位、手动目标、用户添加的候选 host，以及最多 24 个近期 CDN host 的本地成功/失败、真实播放卡顿次数与测速摘要。学习记录 30 天后过期，不包含完整媒体 URL、签名、Cookie 或页面内容。
- 内存中的标签页状态：保存当前标签页最近观测到的 CDN host、B 站当前 `playurl` 响应中签发的媒体 URL、两阶段测速结果、已卡顿候选和少量诊断事件。完整媒体 URL 只留在内存中；关闭标签页、重启扩展后台或退出浏览器后会消失。
- Chrome 会话网络规则：仅对明确的 B 站播放标签页、由 `bilibili.com` 发起且目标属于 `bilivideo.com` 的媒体相关请求生效。规则在浏览器会话结束时清除。

每轮测速会直接从候选 `bilivideo.com` host 读取当前视频的数据：最多 8 个候选各 128 KB 初筛，最快 3 个再各读取最多 1 MB，理论总上限约 4 MB。候选节点会像正常播放一样看到用户的 IP 地址和该媒体请求，但数据不会经过扩展作者的服务器，因为不存在这样的服务器。

扩展不会读取或修改 Cookie，不会访问浏览记录，不会使用代理权限，也不会处理其他网站的请求。

## Chrome Web Store Limited Use

扩展对 B 站播放页和媒体请求信息的使用仅限于提供用户可见的 CDN 观测、测速、切换和卡顿恢复功能。这些信息不会用于广告、画像、信用评估或其他无关用途，不会出售，也不会允许开发者或第三方人工读取。

用户可以随时在弹窗中停用功能。移除扩展会同时清除本地配置与学习记录。

最后更新：2026-07-24。
